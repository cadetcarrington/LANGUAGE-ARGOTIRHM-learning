### 双指针算法
[数的范围二分](数的范围二分.cpp)

[练习799](/algorithmlearning/acwing_training/799.cpp)

### 位运算

1. 先把第k位移到最后一位
2. 个位是几
3.  ``` n >> k & 1```

    [位运算](位运算.cpp)

### lowbit  
lowbit(x): 返回*x*的最后一位1

``` cpp
x & -x = x & (~x + 1)
```

### 离散化（整数）

[离散化](802.cpp)

* 难点 
    1. ```a[ ]```中可能重复元素(**去重**)
    2. 如何算出```a[i]```离散化后的数是多少
* ```cpp
    vector<int> alls;
    sort(alls.begin(), alls.ends());
    alls.erase(unique(alls.begin(), alls.end()), all.end());
    
    int find(int x){
        int l = 0, r = alls.size() - 1;
        while (l < r){
            int mid = l + r >> 1;
            if(alls[mis] >= x){
                r = mid;
            }
            else {
                l = mid + 1;
            }
        }
        return r + 1;
    }

### 区间合并
[803](/algorithm_learning/basic/803.cpp)
快速合并有交集的区间（包括端点）
做法
1. 按区间左端点排序
2. 扫描整个区间，合并可能有交集的